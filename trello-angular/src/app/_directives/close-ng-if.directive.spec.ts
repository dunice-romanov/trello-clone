import { CloseNgIfDirective } from './close-ng-if.directive';

describe('CloseNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
